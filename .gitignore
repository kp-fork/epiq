dist
node_modules

.vscode/tasks.json
.archive
epiq-*.tgz
.DS_Store
coverage

# [epiq]: hydrated state is never to be committed
.epiq

# [epiq]: local hydrated state is never committed
.epiq/events/
.epiq/logs/
